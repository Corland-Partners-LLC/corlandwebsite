# Keeps the public/ directory tracked by git so the Docker build's
# COPY --from=builder /app/public ./public step has something to copy.
# See public/README or the root README's 'How to replace/add images' section.
